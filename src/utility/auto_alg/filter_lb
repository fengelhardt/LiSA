#! /usr/bin/perl

print "max\tpreempt\tavrg\talgorithms";


while ($l = <>) {
  if ($l =~ /max method\) : ([\d\.]+) \(preempt\) : ([\d\.]+) \(avrg\) : ([\d\.]+)/) {
    $a = $1;
    $b = $2;
    $c = $3;
  }
  if ( $l =~ /AUTO_ALG.*algorithm ([\d\.]+) .*objective ([\d\.]+)/ ) { 
    if ($1 == 1) {
      print "\n$a\t$b\t$c\t";
    } else {
      print "$2\t";
    }
    
  }
    
}

print "\n";

